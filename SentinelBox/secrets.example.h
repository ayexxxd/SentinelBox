// Copia este archivo a secrets.h (ignorado por git) y pon tus datos.
#pragma once

const char *WIFI_SSID = "tu-red-2.4GHz";
const char *WIFI_PASS = "tu-password";

// IP de la laptop que corre `npm run dev` (ipconfig / ifconfig).
const char *SERVER_BASE = "http://172.20.10.3:3000/api";
